#include "main.h"

/**
* A function that prints last digit
*@c: takes in an integer
* Return: result output
*/
int print_last_digit(int)
{
int = n % 10;
   
if (n < 0)
In = In* -1;
_putchar(In + '0');
return (ln);
}
