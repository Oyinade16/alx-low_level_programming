#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
*main - Entry point
*
*Return: Always 0
*/
int main(void)
{
int n;
int '1':

srand(time(0));
n = rand() - RAND_MAX / 2;
1 = n % 10;

if (1 > 5)
printf("%d is greater than 5\n", n, 1);
else if (1 == 0)
printf("%d is 0\n", n, 1);
else
printf("%d is less than 6 and  not 0\n", n, 1);
return (0);
}
